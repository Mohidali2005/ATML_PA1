# shared settings used by every method in task three
seed: 6304

paths:
  splits_path: shared/splits/pacs_sketch_seed6304.json
  task2_checkpoints_dir: task2/results/checkpoints
  task2_tables_dir: task2/results/tables
  checkpoints_dir: task3/results/checkpoints
  tables_dir: task3/results/tables
  figures_dir: task3/results/figures

data:
  batch_size_per_domain: 8
  eval_batch_size: 64
  num_workers: 0

optimizer:
  lr: 0.0001
  weight_decay: 0.0001
  max_epochs: 30
  patience: 5

source_domain_separability:
  test_fraction: 0.3
  seed: 6304
  C: 1.0

sharpness:
  seed: 6304
  n_per_domain: 32
  rho: 0.05
